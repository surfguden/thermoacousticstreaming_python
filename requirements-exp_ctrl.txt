# Retained lab environment pins for the hardware-only baseline.
# These versions are carried forward, not newly validated on hardware.
# Python 3.11+. Optional diagnostic dependencies are noted below.
#
# Vendor SDKs are installed separately:
# - WaveForms: Digilent dwf.dll.
# - Qmix: qmix_sdk_for_codex/python wrappers; configure QMIXSDK for runtime DLLs.
# - DCAM: dcamsdk4/samples/python wrappers plus installed dcamapi.dll.
# - Kinesis: .NET assemblies at PiezoStage.kinesis_dir.
#
# python tools/check_environment.py checks Python imports, not device readiness.

Pillow==12.2.0
pyserial==3.5
numpy==2.4.6
pythonnet==3.1.0
clr_loader==0.3.1
mecom @ git+https://github.com/meerstetter/pyMeCom.git@v1.1

# New desktop presentation layer. Startup is simulation-only and does not
# import or initialize any retained hardware driver.
PySide6==6.8.3

# Optional APT discovery and retained Windows environment support.
pylablib==1.4.5
pywin32==312

# Matplotlib is optional for manual_capture_ad2_wavegen_scope_matplotlib.py;
# install separately if that diagnostic is needed.
