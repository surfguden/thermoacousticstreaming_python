# Known Open Items

This register answers one question: **what is still unresolved?**

Current architecture, workflow, routing, inventory, evidence semantics, and the
next project step are in [`project_control.md`](project_control.md). Durable
engineering principles are in [`lessons_learned.md`](lessons_learned.md).
Review/checkpoint provenance is in [`audit_index.md`](audit_index.md).

No item in this document authorizes hardware. Closed software work is not
repeated here as live backlog; the closed-provenance table at the end exists
only to stop settled questions being reopened by accident.

## Categories

| Category | Meaning |
| --- | --- |
| `BLOCKING_BEFORE_HARDWARE` | Must be closed before the relevant energized/physical action. Stops commissioning now. |
| `PHYSICAL_VALIDATION_PENDING` | Software is sufficiently understood; a physical/electrical claim still needs a separately authorized bench step. |
| `NONBLOCKING_SOFTWARE` | Real software debt that does not block the next experiment. |
| `OWNER_DECISION_REQUIRED` | Evidence cannot choose the policy or product preference. |
| `SCIENCE_VALIDATION` | A scientific method/criterion must be defined before the measurement means anything. |
| `OPERATOR_VALIDATION_PENDING` | Software is structurally sound, offline-validated, and independently reviewed; only a real operator watching or performing the workflow during shakedown can judge whether it is a usability problem. Not a defect and not owner-decided. |

An item marked "deferred while disabled" is not a prerequisite for the minimal
camera+AD2 run while that capability stays off.

## BLOCKING_BEFORE_HARDWARE

| ID | Boundary | Closure condition |
| --- | --- | --- |
| **HW-ACOUSTIC-CHAIN-001** | `CUSTOM_ACOUSTIC_AMPLIFIER_CHARACTERIZATION_REQUIRED`. Owner photographs confirm a custom/home-built mains-powered enclosure with IEC entry/switch and external coax/BNC-type connectors, but no commercial identity. Blurry annotations are not gain/bandwidth/impedance evidence. External connector roles, controls, approximate input impedance and gain near 1.9–2.0 MHz, output/load envelope, limiting indication, current transducer identity, installed JP4 state, and same-chain starting evidence are all unresolved. | Search retained build/schematic/PCB/component records — not commercial catalogs. If absent, design a separately reviewed bounded electrical characterization. Any internal inspection is owner/lab-only, powered down and disconnected from mains. **Hard blocker: stop before every energized W1 output.** |
| **HW-AD2-BNC-001** | Official SKU `410-263`, schematic `500-263` rev `C.0`, establishes W1/`J4`/`JP4` and W2/`J5`/`JP5`. `JP4`/`JP5` select a direct path or a 49.9-ohm **series-source** path; they are not 50-ohm shunt loads. Installed PCB revision and jumper positions are unverified. | During a powered-down authorized inspection, retain readable PCB/jumper evidence. Combine the installed series path with the exact downstream input impedance to derive or later measure loaded voltage. Blocks energized W1 together with HW-ACOUSTIC-CHAIN-001. |

## PHYSICAL_VALIDATION_PENDING

| ID | Boundary | Closure condition |
| --- | --- | --- |
| **HW-TIMING-001** | Canonical runtime plans W1/DIO0/DIO1/camera around one PC-triggered software logical t=0, derives the finite DigitalOut run from the achieved DIO0 cadence, and applies External-trigger camera property and timing gates. **The software corrections are closed.** Unmeasured: electrical levels, actual DIO0 edge timing and edge count at the run boundary, first-edge phase, DIO1 electrical window, camera trigger recognition and exposure latency/jitter, LED optical latency, cross-signal alignment on a common timebase, and post-cleanup pin state. | Separately authorized electrical/timing validation with explicit camera mode/polarity and one common-timebase observation. No physical synchronization, exposure-onset, LED-emission, or BNC-onset claim is permitted until then. |
| **HW-PUMP-MOTION-001** | H2B closed stable no-motion startup recovery, not motion readiness. Fresh `position_sensing_initialized=false` fails closed before enable. Installed syringe identity/geometry/loading, available travel, harmless route, fill truth, stop latency, and bounded motion remain unverified. `reference_move()` timeout-stop behavior needs review if reference is actually required. | No-command physical readiness inspection, then a fresh single-client readiness result. Only if the gate requires it, separately review/authorize reference; then validate fill truth, stop latency, and one minimal bounded move, in that order. None of this is implied by tracked fill arithmetic. |
| **HW-VALVE-001** | Software transport is COM5; position 1 → `P01`, position 2 → `P02`. Owner workflow truth identifies P01 as through-chip and P02 as bypass. Protocol acknowledgement and independent physical route observation are different evidence. Valve label is still `MODEL_IDENTITY_REQUIRED`. | If automatic refresh is to be used, separately observe each route with the pump inactive before combining valve and pump actions. |
| **HW-LASER-PATH-001** | Owner truth maps Project Ch2 / API 1 / W2 to iBEAM Analog In/control; DIO1/green is LED timing/control, not laser Digital In. Vendor-family manual: analog modulation is 0…+5 V on channel 2; documented default `sub` polarity reduces power as input rises, so 0 V is not generic optical off and bipolar zero-offset drive may be out of range. Under documented external analog modulation, RS232 Laser ON/OFF and FINE ON/OFF may be unavailable because modulation opens the microprocessor-to-driver connection. Installed option set, polarity, scaling/trim, impedance, and emission behavior are unknown. Production rejects W2; canonical DIO0/DIO1 serve camera/LED only. | Retain installed-unit configuration, then separately design and verify only the bounded control behavior actually required. Never infer optical power from a command voltage. Deferred while software laser control stays disabled. |
| **HW-Z-001** | Current manual Z path is Thorlabs `PPC001`/`PFM450E` through Kinesis. Candidate serial `44533854` is not fresh identity evidence. Controller coordinate zero is not a microscope physical datum. Prior/COM7 is historical only. | Before automated Z use, freshly identify the controller/stage and separately verify direction, scale, travel bounds, and the required physical datum. Deferred while Z stays disabled. |
| **HW-PUMP-FILL-GEOMETRY-001** | Real shakedown Attempt 1 (2026-09-06, `D:\Raw Data\Test`, cross-checked against `logs/hardware_transactions.log`): `configure_syringe()` correctly set a BD 5 ml syringe on the live Qmix backend at 21:45:41 (`max_volume_ml=5.0`); a real pump move completed at 21:46:16 under that geometry; the immediately following `read_fill_level()` returned `6.771902909849169` ml — already outside the syringe it had just itself confirmed as 5 ml. No reference move occurred between the geometry change and that reading. The software-side consequence (a later flush computing an absolute `set_fill_level()` target from that already-invalid value, then rejected by the SDK with `-513`) is now guarded (`Application.flush()`'s `known_capacity_ml` check, `CetoniPump.configure_syringe()`/`known_capacity_ml`) — see the closed-provenance table. **What remains open is the device/SDK behavior itself**: does a syringe-geometry change require a fresh `reference_move()` before `read_fill_level()`/`get_fill_level()` is trustworthy, or is this a Qmix SDK/driver-level inconsistency independent of reference state? Software cannot answer this from logs alone. | Bench-characterize on real hardware: configure a syringe, move to a known position, read fill level immediately (no reference move) versus after a fresh reference move, and compare. Do not guess at Qmix internals from this evidence alone. Deferred while this remains unauthorized as its own hardware session; the software guard closes the immediate risk in the meantime. |

## NONBLOCKING_SOFTWARE

| ID | Boundary | Closure condition |
| --- | --- | --- |
| **TEST-QT-LIFETIME-001** | PySide/Shiboken tests can nondeterministically delete C++ widgets **or hang** during full-window construction/teardown. Observed across several different tests and exception types, and reproduced at the parent commit in an isolated export, so it is not attributable to any recent checkpoint. Affected tests pass in isolation; the full `tests/` suite passes. Retained reproduction: `tests/test_qt_ui_v3.py::test_v3_constructs_on_first_attempt_without_retry`. **Measured baseline (2026-09-05, `QT_QPA_PLATFORM=offscreen`, `-p no:cacheprovider`, external `--basetemp`): 2 failures in 10 identical full-suite runs**, plus two further confirmatory runs after unrelated documentation edits (one clean, one with 2 failures). Three distinct node ids observed: `tests/test_qt_ui_v3.py::test_v3_dialogs_open_at_usable_sizes_without_full_path_width`, `tests/test_qt_ui_hardware_settings.py::test_camera_sequence_group_flags_live_automated_use_and_dead_capture_mode`, `tests/test_qt_ui_hardware_settings.py::test_series_manifest_records_failures_before_and_after_completed_repeats`. Two exception types captured: `SystemError` (NULL widget construction) and `RuntimeError` (deleted C++ object); the third node's exception type was **not captured** because that run used `-q` without `-rf` — recorded as a limitation, not inferred. None of the three reproduces in isolation (8/8 passes each); all three build a full `qt_ui.MainWindow`. At this failure rate a single green full-suite run is weak evidence; a future "suite green" claim should cite this baseline. **The fresh `V3_OPERATOR_WORKFLOW_PRODUCTIZATION` review (2026-09-06) did not independently reproduce the separately reported P2-era full-suite stall**: `tests/test_qt_ui_hardware_settings.py` alone passed 132/132 in isolation, the full offline suite passed 836/1/0 in one clean run, and a deterministic fake-only test confirmed the Qt GUI stayed responsive during a simulated one-second TEC stability wait, with source tracing confirming TEC Apply/Wait dispatches through the established `ActionWorker`/`QThread` path. That P2-era stall's exact pytest node id was never captured, so current evidence keeps it under this existing family rather than opening a new item; see [`lessons_learned.md`](lessons_learned.md) 8.10 for the durable capture discipline the next occurrence must follow. **A new exception type was captured during the fresh Checkpoint-S-closure/hardware-session-lifecycle review (2026-09-07, same offline command shape):** across several consecutive full-suite runs in one session, two runs showed no dot-output progress for 5+ minutes (once stalled after 58%+1 dot, once after 66%+15 dots -- inconsistent stopping points, arguing against a single deterministic culprit test) before being killed rather than timing out on their own; a third run crashed outright with `Windows fatal exception: access violation` (process exit 139), captured mid-`build_with_retry`/`make_window` full `qt_ui.MainWindow` construction at `tests/test_qt_ui_hardware_settings.py::test_qt_ui_save_and_restore_camera_manual_tab_fields` -- a node and construction path this checkpoint's own source changes (`qt_ui_v3.py`, `instruments.py`) never touch. A clean baseline run with none of this checkpoint's changes applied (`git stash` on `qt_ui_v3.py`) completed in 68.9s with 0 failures, and two more clean runs with the changes applied completed in 71-72s (866 passed, 1 skipped, matching baseline plus this checkpoint's 11 new tests) -- consistent with this being the same pre-existing native Shiboken/PySide instability family surfacing under this session's repeated back-to-back full-suite invocations, not a regression this checkpoint introduced. Recorded per 8.10's capture discipline rather than silently folded in: this is the first captured native access-violation/segfault manifestation (previously only `SystemError`/`RuntimeError`/silent-hang were captured) and the first observed case of two different hang stopping-points in the same session. | Reproduce minimally and remove/isolate the ownership fault. **No blanket retries, skips, or xfails** — the failure must stay visible. |
| **SW-V3-NARROW-WINDOW-001** | **Narrowed. Scope: Configure → Acquisition and Configure → Repeat Sample Refresh only** — Review → Timing is closed (see the closed-provenance table); the persistent Execution strip is tracked separately as `SW-V3-EXECUTION-STRIP-WIDTH-001`. At the 980 px `minimumWidth()` floor, measured with the two-row Execution strip in place: Configure → Acquisition `v3CameraSetupScroll` needs 924 px against a 916 px viewport (short 8 px; word-wrapping two V3-only summary labels — "Camera request and canonical trigger plan" — already reduced this from a freshly re-measured 1020 px, itself somewhat higher than the `996 px` figure `62d244f` recorded; not reconciled further). The remaining driver is the "Camera Start Array(s)" sub-panel's spinbox grid, a dense input layout rather than a wrappable label. Configure → Repeat Sample Refresh `v3FluidicsSetupScroll` is unchanged at 960 px (previously 972 px, drift not reconciled); its dominant width comes from `_experiment_flush_group()` (`qt_ui.py`), confirmed via `tools/audit_change_surface.py --symbol _v2_acquisition_group`-style inspection to be genuinely shared with V1. A word-wrap attempt on the shared acquisition grid's own row-title captions was tried and measured to change nothing (924 px before and after), so it was reverted rather than kept as unexplained surface area. Both pages already held containment at the three supported sizes (1366x768, 1440x900, 1920x1080) before and after; only the floor-adjacent region changed. | **Layout redesign is deliberately deferred** for both remaining pages: Acquisition needs the Camera Start Array's spinbox grid rearranged (a real layout change, not a label fix); Fluidics needs a cross-surface edit to shared V1 code, which needs its own review rather than a drive-by inside a usability checkpoint. Do not change V3 geometry merely to reduce an issue count. **`AWAITING_SHAKEDOWN`**: whether either fix is worth doing is an operator-observation judgement — decide after watching someone use these pages during a run, not from the measurement alone. |
| **SW-V3-EXECUTION-STRIP-WIDTH-001** | **The remaining fix (a two-row split) is implemented; residual clipping is now bounded, not eliminated.** Previously one `QHBoxLayout` row needed ~2780-2840 px depending on state, unreachable even at 1920 px, with `current` showing about 29% of its text at 1366 px. Now split into two rows by role — `state`/`last`/`next` (context) on row 1, `current`/`trace` (the two fields whose text length varies most) on row 2 — using the same `_ExecutionFieldLabel` instances, only moved to a new row. **Measured after** (offscreen, fresh): at 1920x1080 and 1440x900 in the ordinary case (AD2 on, trace off), `current` and `trace` now render **completely unclipped** — previously impossible at any width. At 1920x1080 in the worst case (AD2 off, trace degraded), only `current` remains clipped, down from three of five fields. At 1366x768, row 1 (`state`+`last`+`next`, ~1332-1392 px needed) and row 2's `current` still compress meaningfully in both cases; the strip's height grew from ~105 px to a measured 119 px, inside the persistent instrument bar's existing `<= 120 px` contract. Elision (explicit ellipsis) and the full-text tooltip remain the recovery path for whatever clipping is left. | **The two-row option from the prior entry is now implemented; rebalancing stretch further, or splitting `current` onto its own row, remain open options if the residual 1366 px clipping is judged worth closing.** `AWAITING_SHAKEDOWN`: whether that residual is worth pursuing further is an operator-observation judgement — decide after watching someone use the strip during a run, not from the measurement alone. Do not change the strip's geometry merely to reduce an issue count. |
| **UI-PUMP-STOP-THREAD-SAFETY-001** | Real shakedown Section E fix: "Stop pump" now dispatches with `_run_action(..., force=True)`, bypassing the single-worker busy queue so it reaches `self.app.pump.stop()` while a Refill/Empty motion is still in-flight on a separate `QThread`/`ActionWorker`, rather than being silently rejected (`_set_status("Busy")`, never calling `stop()` at all — the real-shakedown-observed defect). This assumes the Qmix SDK's `stop_pumping()` may be safely called from a second thread while a `set_fill_level()` move / `is_pumping()` poll is in flight on the first. Not independently bench-verified; the narrowest available interruption path given the existing single pump-backend instance (no second backend introduced, per the instruction's own constraint). **Extended by `CHECKPOINT_S_CLOSURE_DEBUG_AND_HARDWARE_SESSION_LIFECYCLE_V2` (2026-09-07)**: the new "Shutdown hardware" lifecycle button also reaches `self.app.pump.stop()` through the same `CetoniPump.cleanup()` -> `stop()` -> `backend.close()` authority (proven offline by `tests/test_v3_hardware_session_lifecycle.py::test_v3_shutdown_hardware_reaches_the_same_pump_stop_authority_as_manual_stop`, `::test_v3_shutdown_hardware_disabled_and_refused_while_a_manual_action_is_busy`) -- no second Pump-stop implementation was added, so this item's real-hardware question now covers both callers, not a second one. | Confirm on real hardware that a forced concurrent Stop actually halts an in-progress Refill/Empty motion (not just that the button remains clickable) and that no Qmix API/driver-level fault results from the concurrent call. If the vendor SDK is not safe for this, the interruption path needs redesign; do not simply revert to the pre-fix behavior, which is a confirmed real defect. Bench validation should also exercise Shutdown hardware requested immediately after a long-running Pump motion starts. |
| **UI-EXIT-ACTIVE-WORK-RACE-001** | Independently reviewed while designing `CHECKPOINT_S_CLOSURE_DEBUG_AND_HARDWARE_SESSION_LIFECYCLE_V2`'s "Shutdown hardware" button (2026-09-07): `MainWindow._start_shutdown()` (`qt_ui.py`, shared by both V1 and V3 via `_exit_app()`/`closeEvent()`) fires a graceful-stop request (`self.app.fire_stop_event()`, which the run loop only checks between repeats) and then immediately starts `Application.cleanup()` on a background thread — with no wait for `_busy_count`/`_experiment_series_active`/`_zscan_active`/`_manual_z_operation_active` to clear first. So closing V3 (menu Exit, or the window's close button) while an experiment repeat, a Manual & Service action, or a Z-Scan is genuinely in flight can call `instrument.cleanup()` (stop/reset/close) concurrently with that same worker thread still calling methods on the same backend object — a real use-after/during-close hazard, not merely a theoretical one. Pre-existing behavior, confirmed unchanged since before this checkpoint (not introduced by it, and not touched by it): `_start_shutdown()` itself is reused unmodified by the new Shutdown-hardware button, which deliberately does NOT inherit this gap (Section 16/17 of the task spec) — it gates on exactly those flags before calling `_start_shutdown()`, proven by `tests/test_v3_hardware_session_lifecycle.py::test_v3_shutdown_hardware_disabled_and_refused_while_a_manual_action_is_busy`, `::test_v3_shutdown_hardware_disabled_while_an_experiment_series_is_active`. | **RECORD ONLY.** Not fixed as part of this checkpoint: fixing it means changing `_start_shutdown()` itself, which is shared with Exit and needs its own review of what "wait, then force-close on timeout" should mean for Exit specifically (Exit already has a `_shutdown_timeout_s`/force-close fallback for a genuinely stuck cleanup; adding a *second*, pre-cleanup wait changes Exit's own timing contract). Reproduce with a fake worker holding a backend call open across a `window.close()`/`_exit_app()` and confirm whether the observed hazard is cleanup racing a live call, not just a slow exit. |
| **SW-TRACE-FLUSH-DUPLICATE-001** | The `Flush` step is bracketed **twice** per repeat when automatic refresh runs. Site A is the outer worker bracket in `application.py` `Application._run_experiment2_unfinalized`, which calls `log_action("run", STEP_FLUSH, ... status="STARTED")` before starting the worker thread and `status="COMPLETED"`/`"FAILED"` after the rendezvous. Site B is `application.py` `Application.flush()`, whose body opens `with _report_step(progress, STEP_FLUSH)`; `_report_step` calls `log_action` unconditionally, so the inner bracket fires even though the worker passes `progress=None`. Both emit `subsystem="run"`, `operation="Flush"`, identical statuses. The **only** discriminator is the projected `source` field (`application._run_experiment2_unfinalized` versus `application._report_step`), which is a free-form optional string in `log_action` with no schema constraint or enum — an implicit convention, not an enforced one. The sequence-level initial refresh emits only the inner bracket, so the pattern is also inconsistent between the two refresh kinds. Consequence: any downstream interval analysis over `commissioning_trace.jsonl` or `action_log.jsonl` that pairs `Flush` STARTED to COMPLETED on `(operation, status)` alone will nest or double-count the refresh and compute a wrong duration; it must key on `source`. **Pre-existing runtime behavior, not introduced by the trace layer** — both sites are byte-identical at `ea77bd5` and the commissioning-readiness window's diff touches neither. The trace layer only made it visible. **Real shakedown Attempt 2 investigated whether this item explains the operator-visible duplicate `InitialFlushFailed` line** and found it does not: the initial-flush path (the one both real attempts hit) emits only the inner bracket, exactly as this entry already states, confirmed again against the real `action_log.jsonl` (one `Flush` STARTED, one `Flush` COMPLETED per attempt). The actual duplicate had a different, now-fixed cause — see `SW-TRACE-FLUSH-DUPLICATE-001`'s sibling finding recorded in the closed-provenance table (`_set_status()` re-firing an already-fired progress echo). This entry's own scope and closure condition are otherwise unchanged. | Either give the two brackets distinct operation names, or constrain `source` to a documented vocabulary, or remove one bracket. Do not change the flush control flow to fix a logging shape. |
| **SW-TRACE-IO-COST-001** | `CommissioningTraceRecorder` serializes all writes on a single `threading.RLock`, and `open`/`write`/`flush`/close all execute **inside** that lock (`commissioning_trace.py:160-166`); lock scope is not limited to sequence/buffer mutation. The file handle is opened and closed **per event**, and flushed **per event** — there is no long-lived background writer, so nothing can outlive run finalization. Measured recorder cost (offline, on the verification host, 2026-09-05; indicative, not a specification): median ~152 µs/event, p95 ~215 µs, p99 ~268 µs. The only dispatch filter is `if context is None: return` in `hw_logging.log_action()` (`hw_logging.py:224`); `_report_step` brackets, explicit `log_action` calls, `log_call`, and `log_transaction` all reach the file, and `log_call` costs two records per hardware call (entry + exit). Volume is frame-count independent — 32 trace events for one repeat at 1, 3, 30, and 300 frames — because `HamamatsuDcamBackend.image_sequence()` wraps its whole per-frame loop in one `log_call` (`hamamatsu_dcam.py:384`). Amplification is real-backend-only: `Application.wait_for_pump()` polls `pump.read_status()` every 50 ms (`time.sleep(0.05)`), and `QmixPumpBackend.read_status()` is `log_call`-wrapped (`qmix_backend.py:498`), giving roughly 40 trace records/second for the duration of a real pump move; a 24 s move implies ~960 records and ~146 ms of cumulative trace I/O on the flush worker thread. Consequence not covered by any existing test: because the write is inside the lock, a worker thread writing during a concurrent flush can briefly delay a main-thread `log_action` call, worst case on the order of the measured p99 figure — a cross-thread software coupling, not an extra hardware call. Offline tests use fakes, so the pump poll loop never runs and this is structurally invisible to the current suite. | **`AWAITING_SHAKEDOWN`, and `RECORD ONLY` until then.** Physical commissioning has not begun and the real-hardware magnitude is unknown — the pump poll loop this depends on does not run against fakes, so no amount of further offline work resolves the open question. Changing lock scope or filtering `log_call`/`log_transaction` now would replace measured, known behavior with unvalidated new behavior. Revisit after the first integrated hardware shakedown, once the pump poll loop has actually run against real hardware. |
| **SW-REVIEW-FLUID-PREDICATE-001** | `Application._preflight_automatic_flush_volume()` returns early when `not (self.pump.enabled and self.valve.enabled)` (`application.py:531`), so the runtime charges no aggregate refresh requirement for a sequence that cannot move fluid. `build_result_from_existing_plan()`'s `flush_tracked_fill` check has no equivalent predicate — it demands the full aggregate volume (`TotalExperiments + 1` flushes) against cached tracked fill whenever `request.flush_enabled` is true, regardless of whether pump or valve is actually enabled. Direction: **errs HIGH** — Review never permits a plan the runtime would reject; it can only warn about a plan the runtime will silently skip. Verified by threshold search across flat, partial (some conditions flush-enabled), none, and temperature-expanded-with-one-subgroup-disabled plans: the volume arithmetic agrees exactly with the runtime in every enablement pattern tested; the only divergence is this predicate. The issue is `blocking=False`, so it cannot prevent a Start — usability only. **Inherited, not introduced**: the pre-window single-flush-volume check had the same gap. | **RECORD ONLY.** Add the same `pump.enabled and valve.enabled` predicate to the `flush_tracked_fill` check in `build_result_from_existing_plan()` when this area is next touched; do not fix as a drive-by. |
| **UI-V1-NO-PREFLIGHT-001** | `src/thermo_acoustic/qt_ui.py` — V1, the **current default launcher** (`launch_gui.bat`) — contains no reference to `blocking_issues`, `build_result_from_existing_plan`, or `PreflightIssue` (confirmed: zero matches). The `ad2_disabled` blocking correction in `experiment_planning.py` therefore protects only the V3 Start path (`qt_ui_v3.py:987`, `_v3_start_experiment_with_shared_preflight`); V3 is not the default. What it prevents: at `ea77bd5`, a canonical Start with AD2 disabled already failed with a `ValueError`, but only **after** `experiment.create_folder_and_tdms()` and `experiment.save_settings()` had already run (confirmed at both `ea77bd5` and HEAD), leaving an orphan run directory and a partial `data.tdms` record on disk. The V3 shared preflight now refuses before any of that. This is a new, substantive argument for `UI-V3-DEFAULT-001` — the default launcher lacks a preflight protection the non-default one has — but it does **not** resolve that owner decision. | **RECORD ONLY.** Cross-referenced against `UI-V3-DEFAULT-001`, which remains open and owner-decided; do not treat this entry as resolving it. |
| **SW-NONBLOCKING-FOLLOWUP-001** | Reduced to its one unresolved part. Whether `DCAM_IDPROP_TIMING_MINTRIGGERINTERVAL` already accounts for the currently applied exposure and ROI is **`INSUFFICIENT_EVIDENCE`** from primary vendor documentation. The installed DCAM property reference defines it only as "the period from receiving input trigger to trigger ready", and the exact-model property document's Information column says only "return seconds required minimum trigger interval" with no dependency note — while the same document does carry explicit "Depends on ..." notes for other properties, an absent note is not a statement of independence. Production already reads the property fresh, after the requested ROI and exposure have been applied and read back. Parts (a), (c), (d), (e), (f) and (g) of the original aggregate are closed; see the closed-provenance table. | **`AWAITING_SHAKEDOWN`.** Primary Hamamatsu documentation is silent on the exposure/ROI dependency; only an oscilloscope measurement against the installed camera can settle it — this is not resolvable by more offline reading or reasoning about the documentation that already exists. Resolve only from model-exact Hamamatsu documentation, a vendor statement, or that bench measurement. **Do not invent a formula and do not change runtime to make the question disappear.** The physical half stays with `HW-TIMING-001`. |

## OWNER_DECISION_REQUIRED

| ID | Boundary | Decision needed |
| --- | --- | --- |
| **UI-V3-DEFAULT-001** | V3 is tracked, opt-in, and offline UX-reviewed; V1 remains the default launcher. V2 is retired. V3 has not had an operator-journey evaluation on current hardware. | Decide promotion only after a separate operator/current-hardware evaluation. Until then launch V3 explicitly. |
| **UI-MANUAL-INTERLOCK-001** | V3 separates Manual & Service actions and preserves action-specific confirmations, but the GUI has no universal command-line-style real-hardware acknowledgement gate after initialization. | Owner decides whether existing context/action gates suffice or a global manual/service policy is required. |
| **TEC-AFTER-RUN-POLICY-001** | Investigated as part of `SW-DETERMINISTIC-SHUTDOWN-001` (Checkpoint S, 2026-09-06): nothing in current source automatically disables the TEC output stage after a repeat, condition, or series completes — `Application.set_tec_output_stage_static_off()` exists as an explicit shared action (the Manual & Service "disable output" path) but is not called from any run/repeat/series completion code (confirmed: zero callers in `src/`). Current behavior is therefore implicitly `KEEP_AFTER_RUN`, never a deliberate policy decision. This is unaffected by the Checkpoint-S fix, which only closes the whole-*application*-exit boundary (TEC now explicitly disabled when the application itself closes), not the per-run boundary. | Owner decides whether TEC should remain at target after a successful run/series (useful for multiple measurements at the same temperature, avoiding repeated thermal cycling) or explicitly disable after completion (useful for treating experiment completion as ending active thermal control). Until decided, current `KEEP_AFTER_RUN` behavior is preserved as the scientifically reasonable default; do not change it as a drive-by. |
| **V3 Prepare confirmations** | The Preparation checklist's confirmations are local presentation state. They are correctly labelled as not persisted run evidence and not `PHYSICAL_VERIFIED`. `UNCHANGED` by `V3_OPERATOR_WORKFLOW_PRODUCTIZATION` P1: the new TEC "sample equilibrium confirmed" checkbox added to Prepare's routine temperature group is the same class of local presentation confirmation, not a new evidence stage (Section 13 of that program). Independently re-confirmed by the fresh post-productization review: the checkbox is local UI state only — not `ExperimentRequest`, `RunPlan`, `action_log`, `commissioning_trace`, TDMS, or physical verification — its label states "(local checklist only)" and its tooltip disclaims physical verification; controller stability (`SCI-TEC-EQUIL-001`) remains a distinct claim from sample equilibrium (see [`lessons_learned.md`](lessons_learned.md) 1.4). | Owner decides whether operator preparation confirmations should become durable run evidence; if so it becomes a scoped software item, not a label change. |

## SCIENCE_VALIDATION

| ID | Boundary | Closure condition |
| --- | --- | --- |
| **SCI-TEC-EQUIL-001** | Retained TEC evidence covers communication and narrow controller operations. Controller-reported stability does not establish imaging-plane fluid equilibrium. `UNCHANGED` by `V3_OPERATOR_WORKFLOW_PRODUCTIZATION` P1: Prepare's new routine "Wait for stability" button reports `TecController.wait_until_stable()`'s own tolerance/settle criteria as "ready"/"stable", worded as controller state, and pairs it with a separate local "sample equilibrium confirmed" checkbox rather than inferring equilibrium from controller stability. | Before temperature-controlled scientific claims, define and validate an imaging-plane equilibration criterion or an experimentally justified transfer model. Deferred while TEC stays disabled. |
| **SCI-RHB-CAL-001** | No active Rhodamine-B thermometry workflow links measurements to calibration provenance. | Before thermometry is enabled, retain a compatible calibration identifier/date plus illumination, requested/applied exposure, camera mode/gain, filters, and condition contract. |

## OPERATOR_VALIDATION_PENDING

Opened by the fresh independent review of `V3_OPERATOR_WORKFLOW_PRODUCTIZATION`
(`e433965..5624c2e`, verdict `V3_PRODUCTIZATION_ACCEPTED_WITH_NONBLOCKING_FOLLOWUP`).
Neither item is a defect and neither is owner-decided; both are questions only
a real operator walking the workflow during shakedown can answer. Do not
redesign or move controls to pre-empt either question — observe first.

| ID | Boundary | Shakedown question |
| --- | --- | --- |
| **UI-V3-PREPARE-SCROLL-001** | Prepare's vertical scroll depth was independently measured (offscreen, fresh): at 1366x768, content is 1322x1757 against a 1322x469 viewport (scrollbar range 0..1288, ≈3.75 viewport-heights of traversal); at 1440x900, 1396x1729 against 1396x601 (0..1128, ≈2.88); at 1920x1080, 1876x1572 against 1876x781 (0..791, ≈2.01). `minimumSizeHint()` is 1002x1430 at all three, so the containment floor is unaffected — only the traversal depth changes with viewport height. This is inline routine preparation (7.12/7.21), not a redesign to be pre-judged from the numbers alone. **Re-measured after Checkpoint B's semantic-reduction pass (2026-09-07, same offscreen method)**, following removal of the eight zero-consumer "Local checklist confirmation" checkboxes (Section 6/10 of that checkpoint) and the small net addition of the Prepare-visible syringe-state label: at 1366x768, content dropped to 1322x1682 against a 1322x468 viewport (0..1214, ≈2.59); at 1440x900, 1396x1682 against 1396x600 (0..1082, ≈1.80); at 1920x1080, 1876x1562 against 1876x780 (0..782, ≈1.00 — essentially one viewport height, down from ≈2.01). A real, measured reduction at every supported size from removing unnecessary interaction alone (Section 28's "reduce before reflow" policy) — no structural two-column reflow was needed or attempted, and horizontal containment is unaffected (still passes at all three sizes). | Does the workflow benefit of direct inline preparation outweigh the now-smaller scroll/travel cost, especially at 1366x768 (still ≈2.59 viewport-heights)? Classify from actual shakedown observation as one of `ACCEPTABLE_WORKFLOW_SCROLL`, `HIGH_ROI_LAYOUT_FRICTION`, or `MATERIAL_OPERATOR_FRICTION` — see 7.22. If shakedown still finds 1366x768 materially poor, structural reflow of a specific row (not a whole-page redesign) is the next bounded step, not attempted here. |
| **UI-V3-MANUAL-SERVICE-COMPLETENESS-001** | `V3_OPERATOR_WORKFLOW_PRODUCTIZATION` P1 deliberately promoted routine controls out of Manual & Service into Prepare (pump reference/refill/working position, camera preview/ROI/exposure, routine Z/focus, routine fixed TEC target), leaving Manual & Service with valve routing/manual flush/fault recovery, saved-frame output/sequence/trigger/display, and Z-Scan calibration. The independent review accepted that architecture. No operator has yet exercised Manual & Service for an abnormal scenario since the promotion. | When something abnormal happens — pump fault recovery, unusual/manual pump operation, valve routing, manual flush, failed sample-loading recovery, camera engineering/diagnostic work, Z-scan/calibration, AD2/WFG/MSO engineering work, failed-experiment recovery, or abnormal shutdown/restart — can the operator recover/service the system without discovering that a necessary control disappeared or became unreasonably hard to reach? Do not move routine controls back merely to make Manual & Service look "complete" without an actual observed gap. |

## Closed — provenance only, do not reopen without contradictory evidence

| Topic | Disposition |
| --- | --- |
| Real-operator-workflow simplification (`CHECKPOINT_B_REAL_OPERATOR_WORKFLOW_SIMPLIFICATION_AND_PRODUCTIZATION_CLOSURE_V3`, 2026-09-07) | CLOSED OFFLINE. Fresh consumer audit removed the zero-consumer per-row "Local checklist confirmation" checkboxes (`v3PrepareConfirmed{N}`) from all eight Preparation-checklist rows -- not merely defaulted checked -- while leaving the physical-claim `v3PrepareTecEquilibriumConfirmed` checkbox untouched. Camera ROI confirmed already one canonical authority (`self.roi_h_offset` etc., read by both Prepare's "Apply camera settings" and `_experiment_camera_defaults()`/`ExperimentRequest`); Camera exposure confirmed a deliberate, already-tested, already-source-verified split between manual preview exposure and the scientific run request (not a defect) -- both fields' always-visible labels renamed to "Preview exposure (ms)" to state the distinction without a hover. Syringe configuration UX added a live "selected recipe already applied / differs / UNKNOWN" projection in Prepare (`v3PrepareSyringeState`, `_syringe_selection_matches_applied()`) over the existing session-local `CetoniPump.syringe_config` state -- no live device readback exists (confirmed from `qmix_backend.py`'s own comment) so none was invented, and no new persistent state was added. Found and fixed a genuine Manual-Service completeness gap: V3's own "Stop pump" button was a separate, incomplete reimplementation of `_pump_stop_button()` (missing `force=True` and the `critical_stop` uiRole), and Manual & Service's Pump & Valve panel had no Stop control at all -- both fixed by routing through the shared `_pump_stop_button()` factory in both locations. Prepare's vertical scroll depth measurably reduced at all three supported sizes from the checklist removal alone (see `UI-V3-PREPARE-SCROLL-001`); no structural reflow attempted or needed. Review's blocker/warning/unknown distinction, Configure's scientific-parameter completeness, and the Checkpoint-S hardware-session lifecycle were independently reviewed and found already correct, not touched. **Found and fixed during test development, not shipped**: two of this checkpoint's own new tests triggered a genuine, reproducible (not flaky) native-crash bug -- a QThread dispatched by a real `_run_action()` click whose `finished`/`deleteLater()` chain was still in-flight when the test closed the window, corrupting state visible only in the *next* test's window construction; fixed by waiting for `not window._threads` before `window.close()` in both tests, matching the pattern Checkpoint S's own tests already used. Tests: `tests/test_qt_ui_v3.py` (new: checklist-removal proof, four syringe-state tests plus a failed-configure-is-not-faked test, two V3 Stop-pump tests mirroring V1's existing coverage, one Manual-Service Stop-pump completeness test, three Prepare-scroll-depth regression tests). Full offline suite: 876 passed, 1 skipped, 1 failed (`test_commissioning_trace.py::test_recording_adds_no_backend_calls_with_the_concurrent_refresh_worker`, a monotonic-clock-ordering assertion between two independently-scheduled threads, unrelated to any file this checkpoint touched, passed 3/3 in isolation -- recorded as a timing-sensitive environment flake, not a regression). |
| Hardware-session lifecycle (`CHECKPOINT_S_CLOSURE_DEBUG_AND_HARDWARE_SESSION_LIFECYCLE_V2`, 2026-09-07) | CLOSED OFFLINE. Fresh independent Phase-R review of Checkpoint S found current TEC/AD2/Camera/Valve/Z cleanup already correct except one gap (below); verdict `S_REVIEW_REQUIRES_NARROW_CORRECTION`, corrected, then re-evaluated `ACCEPTED` before Phase H. Added an explicit "Shutdown hardware" button beside "Initialize hardware" in V3's persistent instrument bar (`qt_ui_v3.py` `_v3_instrument_bar()`), reusing the inherited, unmodified `MainWindow._start_shutdown()` coordinator (`close_after=False`) -- the same canonical `Application.cleanup()` authority Exit already used, and the same `CetoniPump.cleanup()` Pump-stop authority manual Stop pump already used. No second cleanup implementation, no second Pump-stop implementation, no second busy/lifecycle authority: gated on the same `_busy_count`/`_experiment_series_active`/`_zscan_active`/`_manual_z_operation_active` flags already used elsewhere (Option A: disabled while busy). Reinitialization already worked via `_initialize_system()`'s pre-existing `Application.cleanup()`-before-rebuild step; this button only exposes it explicitly. See `project_control.md`'s "Hardware-session lifecycle" entry for the full design record. Tests: `tests/test_v3_hardware_session_lifecycle.py` (11 tests: button placement, canonical-cleanup reuse, Pump-stop-authority reuse, active-work gating incl. a genuinely-in-flight fake motion, double-shutdown idempotency, reinitialize-by-subsystem, configuration preservation, stale-connection-indicator clearing, Exit-determinism-after-shutdown). Full offline suite: 866 passed, 1 skipped, 0 failed (clean run; see `TEST-QT-LIFETIME-001` for this session's separately-recorded native-crash evidence from an unrelated, untouched test path). Independently reviewed and recorded, not fixed: `UI-EXIT-ACTIVE-WORK-RACE-001` (Exit's own pre-existing lack of active-work gating before cleanup) and `TEC-AFTER-RUN-POLICY-001` (deliberately not conflated with Shutdown hardware). |
| CetoniPump.cleanup() stop-failure gap (Checkpoint-S-closure narrow correction, 2026-09-07) | CLOSED OFFLINE. `CetoniPump.cleanup()` called `self.stop()` unguarded, so a raising `stop()` skipped `self.backend.close()` entirely (leaking the serial/CAN connection) and skipped `self.initialized = False`. Hardened to the established best-effort multi-step pattern already used by `TecController.cleanup()`/`AD2Sdk.cleanup()`: `stop()` and `backend.close()` are each attempted independently, errors are aggregated and raised together, and `initialized` is left unchanged (not forced False) when cleanup could not be confirmed clean. Tests: `tests/test_application.py::test_cetoni_pump_cleanup_still_closes_backend_when_stop_fails`, `::test_cetoni_pump_cleanup_leaves_initialized_unchanged_when_stop_fails`, `::test_cetoni_pump_cleanup_aggregates_stop_and_close_failures`; confirmed non-vacuous against pre-fix source. |
| SW-PUMP-STALE-FILL-LEVEL-001 (`HW-PUMP-FILL-GEOMETRY-001`'s software half) | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, 2026-09-06). Real Attempt 1 evidence (`D:\Raw Data\Test`, `logs/hardware_transactions.log`) traced a real `-513` SDK rejection to `Application.flush()` computing an absolute `set_fill_level()` target from a `self.pump.fill_level` (6.7719 ml) that already exceeded the syringe capacity `configure_syringe()` itself had just established (5.0 ml) minutes earlier — the device/SDK-level "why" stays open under `HW-PUMP-FILL-GEOMETRY-001`, but the software no longer silently trusts an already-invalid tracked value. Added `CetoniPump.known_capacity_ml` (`instruments.py`, UNKNOWN/`None` until a real `configure_syringe()` sets it — deliberately separate from `max_volume_ml`'s unrelated simulate-mode bookkeeping default, so no existing simulated/test caller is affected) and a narrow `flush()` guard (`application.py`) that raises before any valve/pump command when tracked fill exceeds it. Not a universal `0<=fill<=capacity` Start-time gate. Tests: `tests/test_application.py::test_flush_rejects_a_tracked_fill_level_stale_relative_to_the_known_syringe_capacity`, `::test_flush_is_not_second_guessed_when_the_syringe_capacity_is_unestablished`, `::test_configure_syringe_establishes_known_capacity_from_a_real_backend`. |
| UI-V3-INITIALFLUSH-ERROR-STATE-001 | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, 2026-09-06). Real Attempt 2 (`D:\Raw Data\Test`) showed the V3 Execution indicator ending at `IDLE` after a genuine series failure (`Application.status='InitialFlushFailed'`, Diagnostics: "run_experiment2 did not complete"). Root-cause traced end to end: `Application.flush()`'s `wait_for_pump()` timeout branch (confirmed the actual path here — no valve-position-not-ready event appears between "Waiting for Pump" and the ~5.6 s-later "Flush COMPLETED" in the real evidence) returns `False` without raising; `_report_step(STEP_FLUSH)` reports `step_completed` for **any** non-exceptional return, by its own documented design (see its docstring); `_run_initial_flush_if_required()` then fires `fire_status_event("InitialFlushFailed")` and returns `False`, with no `step_failed` event anywhere — so `_step_states[STEP_FLUSH]` stayed `"completed"` and `_refresh_v3_execution_indicator()` had no fault signal to read. Fixed by mapping `Application`'s own known non-exceptional flush-failure statuses (`FLUSH_NON_EXCEPTIONAL_FAILURE_STATUSES` in `qt_ui.py`: `InitialFlushFailed`, `FlushValvePosition1NotReady`, `FlushValvePosition2NotReady`) to `_step_states[STEP_FLUSH]="failed"` — the same dict `step_failed` itself writes, so `_refresh_v3_execution_indicator()` needed no second consumer. `_report_step()`'s exception-only contract is unchanged; this closes the gap its own docstring named ("a live UI can tell the two apart by also watching the status-event stream") rather than changing that contract. Tests: `tests/test_v3_execution_indicator.py::test_initial_flush_failed_shows_error_not_idle`, `::test_a_flush_completing_normally_is_not_marked_failed_by_an_unrelated_status`. |
| SW-STATUS-ECHO-DUPLICATE-001 | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, 2026-09-06). Investigated the real Attempt 2 operator report of `InitialFlushFailed` appearing twice in the visible status history. `qt_ui.py`'s `_run_experiment_series_body()` echoes `self.app.status` back to the UI via `progress("status", ...)` purely to refresh the display after a repeat, but `_handle_worker_progress`'s generic `"status"` branch always calls `_set_status()`, which both fires a NEW `Application.fire_status_event()` call and displays it — correct for a call site announcing a brand-new status (e.g. "Opening selected hardware"), but wrong for an echo of a value `application.py` already fired, silently double-appending it to `Application.status_events`/`runtime_events`. Confirmed this duplicate does **not** reach the persisted `action_log.jsonl`/commissioning trace (the real evidence shows only one `InitialFlushFailed` record): the echo runs on the GUI thread, outside the worker thread's ContextVar-scoped correlation context `log_action()` requires, so its `log_action()` call is silently a no-op there — itself a notable evidence-integrity observation (a duplicate visible to the operator can be invisible in the persisted trace, and vice versa). Fixed with a new display-only `"status_refresh"` progress kind (`qt_ui.py`) for the two echo call sites (`_run_experiment_series_body`, `_run_temperature_experiment_series`); every other `"status"` call site is unchanged. Test: `tests/test_qt_ui_hardware_settings.py::test_status_refresh_progress_does_not_double_fire_an_already_recorded_status`. Could not fully reconstruct from retained evidence alone why the operator's own transcript shows exactly two consecutive lines rather than one (`HistoryLogWidget.add_entry()` deduplicates identical consecutive rows) — recorded as `INSUFFICIENT_EVIDENCE` for that specific visual count, not as an open defect: the underlying data-layer duplicate is confirmed and fixed regardless. |
| UI-PUMP-STOP-BUSY-QUEUE-001 | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, 2026-09-06). Real operator observation: "Stop pump" did not interrupt an in-progress Refill/Empty. Traced to `_pump_stop_button()` dispatching through `_run_action()` without `force=True`, hitting the same single-worker busy guard as every other pump action (`if self._busy_count and not force: self._set_status("Busy"); return`) — Stop never reached `self.app.pump.stop()` at all while another pump action was in flight. Fixed by dispatching Stop with `force=True`, an existing (previously unused) `_run_action()` parameter that skips the busy check and starts a genuinely separate `QThread`/`ActionWorker` — uses the existing backend/lifecycle, no second Pump backend. Thread-safety of the underlying concurrent Qmix SDK call is an assumption, not bench-verified — tracked as `UI-PUMP-STOP-THREAD-SAFETY-001`. Test: `tests/test_qt_ui_hardware_settings.py::test_stop_pump_reaches_the_backend_while_a_refill_style_motion_is_still_in_progress` (proves the backend receives the stop call before the blocked motion is released, not merely that the button stays clickable). |
| V3 Prepare local checklist confirmation default | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, 2026-09-06). Real operator feedback: the per-row "Local checklist confirmation" checkboxes forced a re-click on every card every session. Traced every consumer before changing anything (source search: no `.isChecked()` reference anywhere in the tree, not Continue-to-Configure, Review, Start, `action_log`, commissioning trace, `ExperimentRequest`/`RunPlan`, or `_settings_dict()`'s persisted fields) and confirmed nothing material depends on the checked state. Defaulted to checked; evidence semantics (label, tooltip: "not persisted run evidence... not physical verification") are unchanged. Deliberately did **not** apply the same default to the separate TEC "sample equilibrium confirmed" checkbox, which makes a specific physical claim rather than a generic acknowledgement (1.4/7.11) — it stays operator-initiated. Test: `tests/test_qt_ui_v3.py::test_v3_prepare_local_checklist_confirmations_default_checked`. |
| TEC target_temperature_c=None presentation | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, 2026-09-06). A second real data point (2026-09-06) showed TEC genuinely reaching a requested ~25 °C target (`output_stage_static_on=True`, `ready=True`, measured within tolerance) while `TecStatus.target_temperature_c` still read `None` — traced to `MeerstetterTecBackend.read_status()` (`tec.py`) faithfully passing through whatever the vendor client's own status dict contains, which this protocol's status query may simply not report; `None` here is `INSUFFICIENT_EVIDENCE` about the vendor protocol, not evidence Apply failed. Prepare's TEC readback (`_refresh_v3_prepare_tec_readback()`) now shows the software's own requested target (`_v3_prepare_tec_last_requested_target_c`, recorded optimistically on Apply, matching the requested/effective split already used elsewhere) alongside, not merged with, the controller's own readback, and reads `target_temperature_c=None` as "not reported by controller protocol" rather than an ambiguous "—". `SCI-TEC-EQUIL-001` (controller stability vs. sample equilibrium) is unaffected. Test: `tests/test_qt_ui_v3.py::test_v3_prepare_tec_readback_shows_requested_target_separately_from_none_controller_readback`. |
| SW-DETERMINISTIC-SHUTDOWN-001 (TEC) | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, Checkpoint S, 2026-09-06). Real operator observation: the TEC remained actively regulating after V3's application/software was closed. Root cause: `TecController.cleanup()` (`tec.py`) only ever closed serial communication (`backend.close()`) — it never commanded the controller out of Static ON, which a Meerstetter TEC otherwise maintains autonomously regardless of link state. Fixed: `cleanup()` now calls the existing `set_output_stage_static_off()` primitive (already bounded/best-effort across channels with readback confirmation) before `backend.close()`, aggregating both errors rather than one silently winning, and preserving the pre-existing invariant that `initialized` stays unchanged when cleanup could not be confirmed clean. Every other subsystem's `cleanup()` (AD2 W1/W2/DigitalOut, Pump, Valve, Camera) was surveyed against the same standard and found already correct with existing test coverage — TEC was the one real gap, matching the one real operator observation. Also fixed in the same checkpoint: `Application.cleanup()` now calls `stop_commissioning_trace()` as a final safety net (a run's own `finally` already covers normal completion/failure/graceful-abort; application exit is a broader boundary nothing previously finalized — see `lessons_learned.md` 3.5). Does not touch per-run/per-repeat TEC policy; see `TEC-AFTER-RUN-POLICY-001` below. Tests: `tests/test_tec.py::test_tec_controller_cleanup_disables_static_output_before_closing_communication`, `::test_tec_controller_cleanup_still_closes_communication_when_output_disable_fails`, `::test_tec_controller_cleanup_is_a_no_op_when_never_connected`; `tests/test_application.py::test_application_cleanup_disables_tec_output_and_still_cleans_up_other_subsystems`, `::test_application_cleanup_finalizes_a_still_recording_commissioning_trace`. |
| TEC Apply target / Z Move wiring re-confirmation | CLOSED OFFLINE (`REAL_SHAKEDOWN_ROUND1_CONSOLIDATED_CORRECTION`, 2026-09-06). Operator reported momentary uncertainty about whether TEC's "Apply target" might actuate the Z stage. Re-confirmed from current source (not the prior review alone): `_v3_apply_prepare_tec_target()` calls only `TecController.apply_static_setpoint()`; `_manual_z_move_to_target()` calls only `stage.set_position()` on `self.app.z_motor.stage`; no shared objectName, no shared signal connection, no cross-reference either direction. Classified visual/semantic adjacency (both rows sit near each other with a numeric target above a button), not a runtime defect. Applied one narrow, low-risk clarity change on top: renamed the button "Apply target" -> "Apply TEC target". Test: `tests/test_qt_ui_v3.py::test_apply_tec_target_and_move_z_target_touch_only_their_own_authority`. |
| SW-TRACE-ABORT-REQUEST-001 | CLOSED OFFLINE at `ba85a4a`. The moment the operator asks to stop is now recorded. `MainWindow._log_operator_stop_requested()` (`qt_ui.py`) emits `operator_stop_requested`, evidence stage `REQUESTED`, from the Abort surface. Not emitted inside `Application.fire_stop_event()`: that primitive is shared with `error_handler_main_loop()` and `cleanup()`, which are not operator intent. The scope question is resolved by thread affinity — `_sequence_evidence_scope()` binds its `action_scope` on the worker QThread and `_abort()` runs on the GUI thread, which cannot see that ContextVar, so the sequence publishes its correlation identity through the existing `progress` path (`sequence_trace_context`) and the abort surface opens a scope from it. The event asserts `run_stopped: false`; `sequence_completed` remains the sole authority on how a sequence ended. Abort while idle correlates to no run and records nothing, asserted rather than implied. Tests: `tests/test_commissioning_trace.py::test_operator_stop_request_is_recorded_against_the_running_sequence`, `::test_stop_request_is_recorded_at_the_ui_boundary_not_in_fire_stop_event`, `::test_operator_stop_request_outside_a_sequence_records_nothing`. |
| SW-V3-SEVERITY-COLLISION-001 | CLOSED OFFLINE at `801f3c8`. `TRACE DEGRADED` now renders `darkorange`, this file's existing divergence marker; `darkred` is reserved for runtime `ERROR` (`qt_ui_v3.py`, `_refresh_v3_execution_indicator()`). Lossy evidence and a failed experiment no longer read the same. `RECORDING` and `OFF` stay quiet — routine status is not an alarm, and no acknowledgement workflow was added. Test: `tests/test_v3_execution_indicator.py::test_trace_degraded_does_not_compete_with_a_runtime_error` forces both conditions simultaneously and asserts the colour tokens differ; `::test_routine_trace_states_stay_quiet` guards against over-alarming the normal case. |
| SW-V3-NARROW-WINDOW-001 (Review → Timing) | CLOSED OFFLINE at `0338e3a`. `_v3_one_repeat_timing_plan()`'s `QGridLayout` needed 1104 px at the 980 px floor, driven entirely by two non-wrapping columns — the "Output" row-title column (420 px for "Laser analog control / W2 (blocked)") and the delta-column header (408 px); the four numeric columns were 60-108 px each and were never the problem. Word-wrapping just those two columns drops the page's minimum to 552 px. No value, semantics, or evidence wording changed. Test: `tests/test_qt_ui_v3.py::test_v3_timing_review_fits_even_at_the_minimum_width_floor`. Scope narrowed on `SW-V3-NARROW-WINDOW-001`, which remains open for the other two pages. |
| SW-TRACE-CONCURRENCY-TEST-001 | CLOSED OFFLINE at `6b5f1ad`. The dense-sequence, monotonic-ordering, event-count-matches-counter, and zero-dropped-events assertions moved from the sequential test into `tests/test_commissioning_trace.py::test_recording_adds_no_backend_calls_with_the_concurrent_refresh_worker`, which exercises the actual concurrent save/flush overlap. Run 9 times with zero flakes, consistent with the 12/12 clean out-of-repository verification this item originally cited. No source changed. |
| V3 Prepare checklist row order vs. owner chronology | CLOSED OFFLINE at `bc51000`. The Preparation checklist's row order contradicted its own long-standing rationale comment and the owner-supplied chronology (init → pump → camera → Z → TEC → other parameters): Environment/Temperature (TEC) sat second, ahead of Guided Pump Preparation and both camera/Z rows. Reordered to Equipment readiness → Guided Pump Preparation → Imaging/Focus → Z/Positioning → Environment/Temperature → Sample/Fluidics → Laser/Optics → Acoustic Precheck, matching Configure's own Acquisition → Conditions tab order (`37088bd`). Navigation-only; every quick-open destination keeps its own established gate. Investigated and confirmed already correct, not touched: Z-scan/single-focus separation, TEC-scan labelling consistency, syringe nested under Guided Pump Preparation rather than a peer object. Test: `tests/test_qt_ui_v3.py::test_v3_reuses_the_supplied_application_and_separates_operator_workspaces` (row-order assertion by object name, 1-based `v3PrepareTaskN`). |
| SW-V3-INDICATOR-LAST-ACTION-001 | CLOSED OFFLINE at `e6a7e0d`. The Execution line now carries `Last`, derived passively from the furthest step marked `"completed"` in `_step_states` over `_v3_execution_steps()` order — the same emitted state the `Next` computation walks. No new canonical event, no elapsed-time inference, no timer. Completed-form wording keeps the evidence discipline ("Camera capture completed", never "camera captured"); the existing forbidden-wording regression covers the field because the shared `indicator()` helper returns it. Strip height unchanged at 105 px at 1366x768, 1440x900 and 1920x1080. Row minimum width grows from 2298 px to 2670 px during a run at 1366x768, extending — not creating — the over-width condition on this strip; reflow stays deferred under `SW-V3-NARROW-WINDOW-001`. Tests: `tests/test_v3_execution_indicator.py::test_last_completed_action_is_derived_from_emitted_completion_events`, `::test_last_completed_action_survives_a_fault_and_shows_the_last_good_step`, `::test_last_completed_action_shows_nothing_meaningful_at_idle`, `::test_execution_line_stays_one_row_per_field`. |
| ARCH-PREFLIGHT-001 | CLOSED. Normal Start is `ExperimentRequest` → independent immutable plan → explicit legacy adapter. The old UI builder is rollback-only; V3 `BuildResult` is presentation/audit only. |
| ARCH-RECORD-001 | CLOSED. Atomic `series_manifest.json` records aggregate lifecycle; TDMS remains per-repeat authority and links to the action stream. |
| SW-ACTION-TRACE-001 | CLOSED OFFLINE. Correlated `action_log.jsonl`, TDMS/manifest linkage, phase-aware diagnostics, bounded vocabulary, and separate primary/cleanup failures. |
| SW-FM-SPAN-001 | CLOSED OFFLINE. AD2 FM index is `100 * half_deviation / center`; 1.909–1.959 MHz gives 50 kHz total span, ±25 kHz, ≈1.2926577 %. Zero/reversed spans fail closed. Historical affected range starts at `d30be02`; no tracked run artifact was available for automatic reinterpretation. Not physical output. |
| SW-FM-SHAPE-001 | CLOSED OFFLINE at `bfd1b31`. Symmetric → Triangle/50 %/bidirectional; RampUp → RampUp/100 %; RampDown → RampDown/100 %. |
| SW-WFG-EVIDENCE-001 | CLOSED OFFLINE at `bfd1b31`. Requested carrier/FM unchanged; post-clamp software-effective SDK arguments stored separately; status is `EFFECTIVE`, never `APPLIED`/`OBSERVED`. |
| SW-SCI-INTEGRITY-001 | CLOSED OFFLINE. Requested FPS is request truth; achieved DIO0 cadence defines the finite DigitalOut run. External acquisition uses fresh minimum-trigger-interval validation; Internal paths keep the documented overlapping `max(exposure, readout)` limit. |
| SW-ACQ-DETERMINISM-001 | CLOSED OFFLINE. Requested ROI applied and freshly read back; enabled CH0 requires `Repeat=1`; FM Sweep conflicts with Frequency Scan. |
| SW-AD2-ROUTING-001 | CLOSED OFFLINE. W1 acoustic with PC trigger; W2 laser Analog In and fails closed; DIO0 camera frame trigger; DIO1 LED timing, never laser Digital In. |
| SW-HANDOVER-SEQUENCE-001 | CLOSED OFFLINE as implemented software policy. One sequence-level initial refresh when enabled; programmed-output completion barrier; concurrent save + hardware-only flush worker; explicit rendezvous; main-thread single-writer TDMS; `FlushCompleted` persisted after rendezvous; separate `PrimaryFailure`/`CleanupFailure`; aggregate `TotalExperiments + 1` tracked-fill preflight before the initial refresh; temperature subgroups do not recharge it. Conservative software timing policy, **not** physical output or fluid proof. Physical prerequisites remain HW-PUMP-MOTION-001 and HW-VALVE-001. |
| SW-V3-UX-001 | CLOSED OFFLINE. Compact persistent state/run controls; Preparation checklist / Configure / Review phases; run-focused Monitor; grouped Manual & Service; Diagnostics. |
| SW-COMMISSIONING-TRACE-001 | CLOSED OFFLINE. `log_action()` is the single canonical software event stream; passive observers project it to the durable commissioning trace and the live indicator. Recording is `OFF`/`RECORDING`/`DEGRADED` over the normal canonical experiment, never a second execution mode; it adds no device call, sleep, barrier, or rendezvous, and a write failure degrades recording without touching control flow. Software evidence only; it establishes no physical timing. |
| SW-V3-EXECUTION-INDICATOR-001 | CLOSED OFFLINE. Persistent read-only Execution line in V3's instrument strip, projecting the canonical progress/event stream with no local timer and no elapsed-time phase inference. Wording is restricted to software facts; cleanup and error remain visible after a run stops. |
| SW-NONBLOCKING-FOLLOWUP-001 (a) achieved-cadence camera gate | CLOSED OFFLINE. The External-trigger feasibility gate validates the software-effective achieved DIO0 spacing when a real `configure_do()` produced one, and falls back to the request otherwise. Requested FPS remains the separate scientific request. Internal/free-running timing is unchanged. Regression uses a real non-exact divider (100 MHz / 30 fps → divider 1 666 666 → 30.000006 Hz). |
| SW-NONBLOCKING-FOLLOWUP-001 (c) TRIGGERTIMES bound | CLOSED OFFLINE. The backend bound is `1..10000`, matching the exact `C15440-20UP` property document. The former `65535` belonged to `MASTERPULSE_BURSTTIMES`, which keeps its own documented range. Canonical value remains 1. |
| SW-NONBLOCKING-FOLLOWUP-001 (d) V3 W2 contextual note | CLOSED OFFLINE. The authoritative blocked-plan note names Laser / W2 (Project Ch2) and stays visible whether or not Acoustic/W1 is enabled. Presentation only; W2 planner and runtime semantics are unchanged and W2 remains blocked. |
| SW-NONBLOCKING-FOLLOWUP-001 (e) V3 overflow assertions | CLOSED OFFLINE as a test-honesty correction, with no layout change. `horizontalScrollBar().maximum()` is structurally 0 on pages whose content carries `QSizePolicy.Ignored`, so it cannot detect clipping there; those pages now assert that the content's `minimumSizeHint().width()` fits the viewport, which the Ignored policy does not suppress. The Review page keeps a real horizontal policy and retains the scrollbar-extent assertion as well. |
| SW-NONBLOCKING-FOLLOWUP-001 (f) AD2-disabled consistency | CLOSED OFFLINE. With the canonical trigger architecture in the plan, `ad2_disabled` is a blocking preflight issue and the V3 readiness chip reports that the subsystem is required and the runtime fails closed. The runtime AD2-required hardware gate itself is unchanged. |
| SW-NONBLOCKING-FOLLOWUP-001 (g) aggregate refresh in Review | CLOSED OFFLINE. Review derives the aggregate requirement from the plan — one refresh per flush-enabled condition plus one sequence-level initial refresh, counted once across temperature groups — and compares it to cached tracked fill. It stays a warning because the evidence is cached; the runtime applies the same gate against live tracked state at Start. No new pump read. |
| TOOL-VALVE-PORT-001 | CLOSED OFFLINE. The manual full-workflow plan/CLI and the shared hardware factory accept only valve resource `COM5`; `COM6` is hard-rejected as the TEC path. Physical routing remains HW-VALVE-001. |
| HW-QMIX-CAN-001 startup recovery | CLOSED FOR NO-MOTION STARTUP. H2A/H2B evidence. Does not close HW-PUMP-MOTION-001. |
| Generic production DO Clock | SUPERSEDED. The legacy DO-clock helper and its generic configuration are unused by normal production. Canonical production programs one finite shared PC-triggered DigitalOut program: DIO0 the camera `EXT.TRIG` train, DIO1 the LED timing window. Neither is laser Digital In; neither is physical timing verification. |
| CH2 "unused" | SUPERSEDED. W2 is laser Analog In. Electrical semantics remain HW-LASER-PATH-001. |
| DIO1 laser-vs-LED identity conflict | SUPERSEDED / OWNER_ADJUDICATED. DIO1/green is LED timing/control; DIO0/pink is camera `EXT.TRIG`. Canonical production programs both for camera/LED timing; neither carries laser control. |
| Two-pump profile | SUPERSEDED. One-pump configuration; the legacy two-pump profile is rejected. |
| Valve on COM6 | SUPERSEDED ERROR. Valve is COM5; COM6 is the TEC path. |
| Prior COM7 as current Z | OBSOLETE. Current Z uses Thorlabs/Kinesis. |
| ROI inherited from manual state | SUPERSEDED. Normal plan carries requested ROI; runtime applies it and reads it back. |
| Exposure request equals applied value | SUPERSEDED. `RequestedExposureMs` and `AppliedExposureMs` are separate. |
| Duplicate post-P02 pump target | CLOSED. One target command before P02; wait only after P02. |
| AnalogOut-only deterministic cleanup | SUPERSEDED BY EXTENSION. Cleanup now also stops and resets DigitalOut before device close, because canonical production programs DIO0/DIO1. |
| Pre-redesign V3 everything-dashboard | SUPERSEDED. Current V3 operator model is in `project_control.md` and current source/tests. |

## Retained evidence navigation

Point-in-time and historical material is indexed in
[`audit_index.md`](audit_index.md). The records most often needed alongside this
register are [`p0_hardware_truth_20260828.md`](p0_hardware_truth_20260828.md),
[`hardware_repair_plan.md`](hardware_repair_plan.md),
[`qt_lifetime_investigation.md`](qt_lifetime_investigation.md), and the retained
`runs/qmix_h1_*`, `runs/qmix_h2a_*`, `runs/qmix_h2b_*` evidence. Retained
evidence is not permission to repeat or broaden a hardware session.

## Immediate project gate

Close HW-ACOUSTIC-CHAIN-001 and HW-AD2-BNC-001: retain the custom-amplifier
minimum characterization envelope, installed JP4 position, cable route, current
transducer identity, and a defensible same-chain starting amplitude. Offline
software closure does **not** close the physical acoustic chain. Do not energize
W1 or resume commissioning Gate 3/Gate 4 until that closure is explicit and the
run is separately authorized. Camera-only Gate 2 is accepted and must not be
repeated.
